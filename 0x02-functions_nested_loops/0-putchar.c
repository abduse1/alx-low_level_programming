#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int  m = '_', n = 'p', o = 'u', q = 't', s = 'c', v = 'h', w = 'a', y = 'r';
putchar(m);
putchar(n);
putchar(o);
putchar(q);
putchar(s);
putchar(v);
putchar(w);
putchar(y);
putchar('\n');
return (0);
}
