#include "main.h"

/**
 *  _strlen -find the length of a string 
 *
 * @c: To count the length of a string
 * Return - string length
int _strlen(char *s)
{
	int c = 0;
	for (; *s != '\0'; s++)
	{ 
		c++;
	}
	
	return (c);
}
